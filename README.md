# PoE2 Easy Arbitrage Solver With Editable Table

Main file:

```text
poe2_easy_arbitrage_solver_table.py
```

Run:

```bash
pip install -r requirements.txt
streamlit run poe2_easy_arbitrage_solver_table.py
```

This version replaces the big paste box with an editable table with lines.
